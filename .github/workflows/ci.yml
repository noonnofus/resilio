name: CI

on:
  pull_request:
  push:
    branches: [main]

jobs:
  quality:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: pnpm/action-setup@v6
        with:
          version: 11.3.0
      - uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm exec playwright install --with-deps chromium
      - run: pnpm check

  next-compatibility:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        next-version: [15.5.19, 16.2.7]
    steps:
      - uses: actions/checkout@v6
      - uses: pnpm/action-setup@v6
        with:
          version: 11.3.0
      - uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm --filter @resiliojs/next add --save-dev next@${{ matrix.next-version }} --lockfile=false
      - run: pnpm build
      - run: pnpm --filter @resiliojs/next typecheck
      - run: pnpm --filter @resiliojs/next test
